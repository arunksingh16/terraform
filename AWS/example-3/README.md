Apply Output 

```
 # aws_iam_user.iam_accnt[0] will be created
  + resource "aws_iam_user" "iam_accnt" {
      + arn           = (known after apply)
      + force_destroy = false
      + id            = (known after apply)
      + name          = "accnt-0"
      + path          = "/"
      + tags_all      = (known after apply)
      + unique_id     = (known after apply)
    }

  # aws_iam_user.iam_accnt[1] will be created
  + resource "aws_iam_user" "iam_accnt" {
      + arn           = (known after apply)
      + force_destroy = false
      + id            = (known after apply)
      + name          = "accnt-1"
      + path          = "/"
      + tags_all      = (known after apply)
      + unique_id     = (known after apply)
    }

  # aws_iam_user.iam_accnt[2] will be created
  + resource "aws_iam_user" "iam_accnt" {
      + arn           = (known after apply)
      + force_destroy = false
      + id            = (known after apply)
      + name          = "accnt-2"
      + path          = "/"
      + tags_all      = (known after apply)
      + unique_id     = (known after apply)
    }

Plan: 3 to add, 0 to change, 0 to destroy.

```
